name: Build APK from Zip

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 1) 7z ile kök yolları görmezden gelerek çıkar
      - name: Extract download.zip (7z)
        run: |
          7z x download.zip -y -oextracted || true
          # 7z kök yolları otomatik düzeltir, içerik ./extracted/ içine gelir

      # 2) İç içe tar.gz varsa onu da aç
      - name: Extract nested tar.gz
        run: |
          if [ -f "extracted/ultra-download-manager-complete.tar.gz" ]; then
            mkdir -p nested
            tar -xzf extracted/ultra-download-manager-complete.tar.gz -C nested
          fi

      # 3) Otomatik proje klasörünü bul (gradlew dosyasına göre)
      - name: Locate Android project
        id: proj
        run: |
          DIR=$(find . -name "gradlew" -type f -exec dirname {} \; | head -n1)
          echo "PROJECT_DIR=$DIR" >> $GITHUB_ENV
          echo "dir=$DIR" >> $GITHUB_OUTPUT

      # 4) Debug – bulduğumuz klasörü göster
      - name: Show project tree
        run: |
          echo "Detected project dir: ${{ env.PROJECT_DIR }}"
          ls -lah "${{ env.PROJECT_DIR }}"

      # 5) Gradle wrapper’ı çalıştırıp APK’yı üret
      - name: Build Debug APK
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --stacktrace

      # 6) APK’yı artifacts olarak yükle
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: ${{ env.PROJECT_DIR }}/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 7
          
